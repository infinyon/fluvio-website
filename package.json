{
  "name": "netlify",
  "version": "1.0.0",
  "description": "adding package directory to the top to help netlify load the depencies",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "netlify",
    "serverless",
    "typescript"
  ],
  "author": "Netlify",
  "license": "MIT",
  "dependencies": {
    "@netlify/functions": "^1.0.0",
    "@types/node": "^14.0.0",
    "node-fetch": "^3.2.3",
    "typescript": "^4.0.0"
  }
}
