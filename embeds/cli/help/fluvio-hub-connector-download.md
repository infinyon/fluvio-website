```
Download SmartConnector to the local folder
Usage: fluvio hub connector download [OPTIONS] <name>
Arguments:
  <name>  SmartConnector name: e.g. infinyon/salesforce-sink@v0.0.1
Options:
  -o, --output <PATH>    Target local folder or file name
      --target <TARGET>  Target platform for the package. Optional. By default the host's one is used [default: aarch64-apple-darwin]
  -h, --help             Print help (see more with '--help')
```