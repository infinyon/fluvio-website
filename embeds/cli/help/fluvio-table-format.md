```
Create a TableFormat display specification

Used with the consumer output type `full_table` to describe how to render JSON data in a tabular form

Usage: fluvio table-format <COMMAND>

Commands:
  create  Create a new TableFormat display
  delete  Delete a TableFormat display
  list    List all TableFormat display

Options:
  -h, --help
          Print help (see a summary with '-h')
```